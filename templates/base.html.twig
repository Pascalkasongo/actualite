<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AcadémiaNews</title>
    <link rel="stylesheet" href="{{ asset('build/app.css') }}">
</head>

<body class="bg-gray-50">

<!-- NAVBAR -->
<nav class="bg-red-600 shadow-md">
    <div class="max-w-7xl mx-auto px-4">

        <div class="flex justify-between items-center h-16">

            <!-- LOGO -->
            <a href="{{ path('app_home') }}" class="text-xl font-bold text-white">
                Théologie Actualité
            </a>

            <!-- DESKTOP MENU -->
            <div class="hidden md:flex items-center">

                <div class="flex items-center gap-8">

                    <a href="{{ path('app_home') }}"
                      class="text-white hover:text-gray-200 font-medium transition">
                        Accueil
                    </a>

                    {% if app.user and is_granted('ROLE_ADMIN') %}
                        <a href="{{ path('app_actualite_index') }}"
                          class="text-white hover:text-gray-200 font-medium transition">
                            Actualités
                        </a>
                    {% endif %}

                    <!-- DROPDOWN -->
                    <div class="relative">
                        <button id="formation-btn"
                            class="flex items-center gap-1 text-white hover:text-gray-200 font-medium transition">

                            Formation
                            <svg id="arrow"
                                class="w-4 h-4 transition-transform"
                                fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-width="2" d="M19 9l-7 7-7-7"/>
                            </svg>
                        </button>

                        <div id="dropdown"
                            class="hidden absolute left-0 top-full mt-3 w-48 bg-white rounded-lg shadow-lg z-50">

                            <a href="{{ path('app_enseignant') }}"
                              class="block px-4 py-2 text-gray-700 hover:bg-gray-100">
                              Enseignants
                            </a>

                            <a href="#"
                              class="block px-4 py-2 text-gray-700 hover:bg-gray-100">
                              Étudiants
                            </a>
                        </div>
                    </div>

                    <a href="https://bibliotheque-udbl.org/pmb/opac_css/"
                      class="text-white hover:text-gray-200 font-medium transition">
                        Bibliothèque
                    </a>

                    <a href="{{ path('app_apropos') }}"
                      class="text-white hover:text-gray-200 font-medium transition">
                        À propos
                    </a>

                </div>

</div>

            <!-- MOBILE BUTTON -->
            <button id="menu-btn" class="md:hidden text-white text-2xl">
                ☰
            </button>

        </div>

        <!-- MOBILE MENU -->
        <div id="mobile-menu" class="hidden md:hidden pb-4">

            <a href="{{ path('app_home') }}" class="block text-white py-2">
                Accueil
            </a>

            {% if app.user and is_granted('ROLE_ADMIN') %}
                <a href="{{ path('app_actualite_index') }}" class="block text-white py-2">
                    Actualités
                </a>
            {% endif %}

            <!-- MOBILE DROPDOWN -->
            <div>
                <button id="mobile-formation-btn"
                    class="w-full flex justify-between text-white py-2">

                    Formation
                    <span id="mobile-arrow">▼</span>
                </button>

                <div id="mobile-dropdown" class="hidden pl-4">

                    <a href="{{ path('app_enseignant') }}" class="block text-white py-1">
                        Enseignants
                    </a>

                    <a href="#" class="block text-white py-1">
                        Étudiants
                    </a>

                </div>
            </div>

            <a href="https://bibliotheque-udbl.org/pmb/opac_css/" class="block text-white py-2">
                Bibliothèque
            </a>

            <a href="{{ path('app_apropos') }}" class="block text-white py-2">
                À propos
            </a>

        </div>

    </div>
</nav>

